<div class="potin">

<p> Ceci est la page du potin n°<%= (params[:id].to_i)%> <p/>

<p>  
 
	
	
	
	 <br>
	 <div class="card" style="width: 30rem;">
    <div class="card-body">
    <h5 class="card-title"><%= @gossip.title%></h5>
</br>	
    <p class="card-text"><%= @gossip.content%></p>
    <p class="btn btn-primary"> <%= link_to @gossip.user.first_name, user_path(:id => @gossip.user_id)%></p>
     <p> <%= link_to "Ville : "+@gossip.user.city.name, city_path(:id => @gossip.user.city_id)%></p>
     <% if is_owner?(@gossip.user.id) %>
     <p> <%= link_to "Editer", edit_gossip_path(:id => @gossip.id)%></p>
     <p> <%= link_to "Supprimer", gossip_path(:id => @gossip.id),   data: { confirm: 'Are you sure?' }, method: :delete %></p>
     
     <% end %>



  </div>
</div>

	 <br>
	 <p/>
	</div>






 <% if flash[:notice] %>
  <div class="alert alert-dismissible alert-danger">
    <div class="notice"><strong><%= flash[:notice] %></strong>
     </div>
    <button type="button" class="close" data-dismiss="alert">&times;</button>
  </div>
<% end %>
    <!-- ... -->
  